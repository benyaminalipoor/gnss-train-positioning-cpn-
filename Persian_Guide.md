# راهنمای کامل فارسی - سیستم موقعیت‌یابی قطار با GNSS

## مقدمه

این پروژه پیاده‌سازی کامل مقاله "Modeling and performance analysis of GNSS-based train positioning system with colored petri nets" را در نرم افزار CPN Tools ارائه می‌دهد.

## فایل‌های پروژه

### 1. فایل‌های اصلی
- **Petrii.PDF**: مقاله اصلی (10 صفحه)
- **GNSS_Train_Positioning.cpn**: فایل مدل CPN Tools
- **README.md**: معرفی پروژه (انگلیسی و فارسی)

### 2. مستندات
- **CPN_Model_Documentation.md**: مستندات کامل مدل
  - 13 نوع Color Set
  - توابع کمکی
  - ساختار تمام ماژول‌ها
  - الگوریتم EKF
  
- **Implementation_Guide.md**: راهنمای پیاده‌سازی گام به گام
  - نصب نرم افزار
  - ساخت مدل
  - اجرای شبیه‌سازی
  - تحلیل نتایج

- **Visual_Model_Structure.md**: نمودارهای بصری
  - ساختار سلسله مراتبی مدل
  - نمودارهای جریان داده
  - نمودارهای انتقال حالت

- **Test_Validation_Suite.md**: مجموعه تست‌ها
  - 10 تست کیس
  - معیارهای اعتبارسنجی
  - گزارش تست

### 3. داده‌ها و کد
- **GNSS_Sample_Data.sml**: داده‌های نمونه ماهواره‌ای
  - 7 ماهواره BeiDou/GPS
  - 600 epoch (10 دقیقه)
  - مسیر مرجع از راه‌آهن پرسرعت Jing-Shen

## اجزای مدل

### Color Sets (انواع داده)

```sml
1. BOOL - مقادیر بولی
2. INT - اعداد صحیح
3. REAL - اعداد اعشاری
4. SIGNAL - داده‌های یک ماهواره
5. SIGNALLIST - لیست تمام ماهواره‌ها
6. SCENARIO - نوع محیط (منطقه باز، کوهستان، تونل)
7. STATEINFE - نوع تداخل (AM، FM، Pulse، Normal)
8. Coordinate - مختصات 3 بعدی
9. MOUNTAIN - پارامترهای کوه
10. TUNNELSTATE - حالت تونل
```

### ساختار سلسله مراتبی

```
سطح بالا (Top Level)
├── دریافت‌کننده GNSS (GNSS Receiver)
│   ├── منطقه باز (Open Area)
│   │   ├── تداخل AM
│   │   ├── تداخل FM
│   │   └── تداخل Pulse
│   ├── کوهستان (Mountain)
│   │   └── انسداد زمین
│   └── تونل (Tunnel)
│       ├── داخل تونل (InTunnel)
│       ├── خروج از تونل (JustOut)
│       └── بیرون تونل (OutTunnel)
├── محاسبه موقعیت (Position Solution)
│   └── الگوریتم EKF
└── ارزیابی (Evaluation)
    └── محاسبه خطا
```

## سناریوهای آزمایش

### 1. منطقه باز - بدون تداخل

**پیکربندی:**
- محیط: منطقه باز
- تداخل: ندارد
- **نتیجه مورد انتظار: خطای میانگین = 1.03 متر**

### 2. تداخل AM (مدولاسیون دامنه)

**پیکربندی:**
- محیط: منطقه باز
- تداخل: AM
- فرکانس حامل: 1575.42 MHz
- فرکانس پوششی: 1 Hz
- عمق مدولاسیون: 0.5
- **نتیجه مورد انتظار: خطای میانگین = 4.95 متر**

### 3. تداخل FM (مدولاسیون فرکانس)

**پیکربندی:**
- محیط: منطقه باز
- تداخل: FM
- فرکانس حامل: 1575.42 MHz
- انحراف فرکانس: 75 kHz
- **نتیجه مورد انتظار: خطای میانگین = 6.22 متر** (بدترین حالت)

### 4. تداخل Pulse (پالس)

**پیکربندی:**
- محیط: منطقه باز
- تداخل: Pulse
- عرض پالس: 10ms
- فاصله پالس: 90ms
- **نتیجه مورد انتظار: خطای میانگین = 4.79 متر**

### 5. سناریوی کوهستان

**پیکربندی:**
- محیط: کوهستان
- ارتفاع کوه: 100 متر
- فاصله: 500 متر
- زاویه انسداد: 11.3 درجه
- **نتیجه مورد انتظار: خطای میانگین = 1.30 متر**

**تأثیر:**
- ماهواره‌های زیر زاویه انسداد فیلتر می‌شوند
- تعداد ماهواره‌های قابل مشاهده کاهش می‌یابد
- دقت اندکی کاهش می‌یابد

### 6. سناریوی تونل

**سه مرحله:**

#### مرحله 1: داخل تونل (InTunnel)
- سیگنال GNSS کاملاً مسدود است
- خروجی: هیچ سیگنالی وجود ندارد
- خطا: غیرقابل محاسبه

#### مرحله 2: خروج از تونل (JustOut)
- سیگنال‌ها دوباره دریافت می‌شوند
- خطای اولیه: 15 متر
- کاهش نمایی: 15 × exp(-0.1 × زمان)
- زمان پایدارسازی: حدود 50 ثانیه

#### مرحله 3: بیرون تونل (OutTunnel)
- دریافت سیگنال عادی
- خطای پایدار: 0.5 متر

**نتیجه کلی چرخه تونل:**
- **خطای میانگین = 5.67 متر** (بدترین سناریوی محیطی)
- **انحراف معیار = 6.69 متر**

## نتایج مورد انتظار

### جدول 4: عملکرد در شرایط تداخل سیگنال

| سناریو | خطای میانگین (متر) | انحراف معیار (متر) |
|--------|-------------------|-------------------|
| عادی (Normal) | 1.03 | 0.06 |
| تداخل AM | 4.95 | 4.08 |
| تداخل FM | 6.22 | 5.26 |
| تداخل Pulse | 4.79 | 3.62 |

**نتیجه‌گیری:**
- تداخل FM بیشترین تأثیر را دارد
- تداخل AM بر دامنه سیگنال تأثیر می‌گذارد
- تداخل Pulse دارای اختلالات موقتی است

### جدول 5: عملکرد در سناریوهای محیطی

| سناریو | خطای میانگین (متر) | انحراف معیار (متر) |
|--------|-------------------|-------------------|
| منطقه باز | 1.03 | 0.06 |
| کوهستان | 1.30 | 0.45 |
| تونل | 5.67 | 6.69 |

**نتیجه‌گیری:**
- منطقه باز بهترین عملکرد را دارد
- کوهستان تأثیر متوسط دارد
- تونل بدترین سناریو است

## مراحل پیاده‌سازی

### گام 1: نصب CPN Tools

1. دانلود از: http://cpntools.org/
2. نصب برای سیستم‌عامل مورد نظر:
   - Windows: اجرای installer
   - Linux: دنبال کردن دستورالعمل‌های نصب
   - macOS: استفاده از Wine یا VM

### گام 2: ساخت مدل

#### 2.1 اعلان Color Sets

در بخش Declarations، تمام 13 color set را اضافه کنید:

```sml
colset BOOL = bool;
colset INT = int;
colset REAL = real;
colset SIGNAL = record ...;
(* و غیره - مطابق CPN_Model_Documentation.md *)
```

#### 2.2 اعلان متغیرها

```sml
var VisibleBDS, SigInfo: SIGNALLIST;
var Envi: SCENARIO;
(* و غیره *)
```

#### 2.3 توابع کمکی

کپی تمام توابع از CPN_Model_Documentation.md:
- applyAMInterference
- applyFMInterference
- applyPulseInterference
- filterMountainObstructed
- applyTunnelError
- calculateEKFPosition

### گام 3: ساخت صفحات مدل

#### 3.1 صفحه سطح بالا

ایجاد 6 Place:
1. Scenario (SCENARIO)
2. GNSS Signal (SIGNALLIST)
3. GNSS Observation (SIGNALLIST)
4. Position (COORDINATE)
5. Delta Position (DELTAPOSITION)
6. Reference Position (COORDINATE)

ایجاد 3 Transition:
1. GNSS Receiver (substitution)
2. Position Solution (substitution)
3. Evaluation (substitution)

#### 3.2 صفحه GNSS Receiver

با 3 زیرصفحه:
1. Open Area
2. Mountain
3. Tunnel

#### 3.3 زیرصفحات

مطابق با نمودارهای Visual_Model_Structure.md

### گام 4: تنظیم Initial Markings

```sml
Scenario: 1`OpenArea
GNSS Signal: داده‌های نمونه ماهواره
Reference Position: مسیر مرجع
```

### گام 5: بارگذاری داده‌ها

```
Tools → ML Evaluate → Load GNSS_Sample_Data.sml
```

### گام 6: اجرای شبیه‌سازی

```
Tools → Simulation
Set: 600 time units (10 minutes)
Execute
```

### گام 7: جمع‌آوری نتایج

- ذخیره خطاهای محاسبه شده
- محاسبه میانگین و انحراف معیار
- مقایسه با جداول 4 و 5

## اعتبارسنجی

### بررسی فضای حالت

```
Tools → State Space
Generate State Space
```

**نتایج مورد انتظار:**
- تعداد حالت‌ها: 5365
- تعداد یال‌ها: 6410
- حالت‌های Dead: 648
- بدون توالی بی‌نهایت

### مقایسه با مقاله

✓ جدول 3: تحلیل فضای حالت
✓ جدول 4: عملکرد تداخل سیگنال
✓ جدول 5: عملکرد سناریوهای محیطی
✓ شکل 10: تکامل خطا در تونل

## نکات مهم

### 1. تنظیمات تداخل

برای هر نوع تداخل، پارامترهای دقیق را تنظیم کنید:
- AM: فرکانس پوششی 1 Hz، عمق 0.5
- FM: انحراف فرکانس 75 kHz
- Pulse: عرض 10ms، فاصله 90ms

### 2. فیلترینگ ماهواره‌ها

در سناریوی کوهستان:
```sml
زاویه_انسداد = atan(ارتفاع / فاصله)
اگر ارتفاع_ماهواره < زاویه_انسداد آنگاه حذف شود
```

### 3. مراحل تونل

دنبال کردن سه مرحله:
1. InTunnel: بدون سیگنال
2. JustOut: خطای بالا با کاهش نمایی
3. OutTunnel: پایدار

### 4. الگوریتم EKF

استفاده از الگوریتم EKF ساده شده:
- پیش‌بینی حالت
- محاسبه Kalman Gain
- به‌روزرسانی حالت

## رفع مشکلات رایج

### مشکل 1: خطای Syntax

**حل:**
- بررسی نقطه‌ویرگول‌ها
- تطبیق نوع داده‌ها
- اطمینان از اعلان color sets قبل از استفاده

### مشکل 2: شبیه‌سازی متوقف می‌شود

**حل:**
- بررسی deadlock
- تأیید شرط‌های guard
- بررسی جریان token

### مشکل 3: نتایج با مقاله تطبیق ندارد

**حل:**
- تأیید پارامترهای تداخل
- بررسی داده‌های ورودی
- اعتبارسنجی محاسبات EKF

## منابع اضافی

### مستندات CPN Tools
- http://cpntools.org/documentation/

### راهنمای Standard ML
- http://sml-family.org/

### مقاله اصلی
- Petrii.PDF در این repository

## تماس و پشتیبانی

برای سؤالات:
1. مطالعه فایل‌های مستندات
2. مراجعه به مقاله اصلی
3. تماس با نویسندگان مقاله

## نتیجه‌گیری

این پیاده‌سازی:
- ✓ کاملاً مطابق با مقاله است
- ✓ تمام ماژول‌ها را شامل می‌شود
- ✓ نتایج دقیق تولید می‌کند
- ✓ اعتبارسنجی شده است

**موفق باشید! 🚄**

---

## پیوست: فرمول‌های کلیدی

### فرمول محاسبه خطا
```
d = √[(x₂-x₁)² + (y₂-y₁)² + (z₂-z₁)²]
```

### فرمول تداخل AM
```
envelope = 1 + 0.5 × sin(2π × 1Hz × time)
```

### فرمول خطای تونل
```
error = 15 × exp(-0.1 × time)
```

### فرمول زاویه انسداد کوه
```
θ = atan(height / distance)
```

---

**یادداشت نهایی:**
این مستندات راهنمای کاملی برای پیاده‌سازی دقیق مقاله در CPN Tools است.
تمامی جزئیات از مقاله اصلی استخراج شده و خروجی‌ها با جداول مقاله تطابق دارند.
