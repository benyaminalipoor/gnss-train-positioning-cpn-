# خلاصه پیاده‌سازی - GNSS Train Positioning

## مقدمه

این پروژه شبیه‌سازی کامل سیستم موقعیت‌یابی قطار مبتنی بر GNSS با استفاده از شبکه‌های پتری رنگی (Colored Petri Nets) و اتوماتون را پیاده‌سازی کرده است. تمام اشکال، جداول و نتایج مقاله به طور دقیق بازتولید شده‌اند.

## فایل‌های اصلی

### 1. gnss_train_positioning_simulation.py
فایل اصلی شبیه‌سازی که شامل موارد زیر است:
- ساختارهای داده (Data Structures)
- مدل تولید سیگنال GNSS
- مدل‌های تداخل (AM, FM, Pulse)
- سناریوهای محیطی (منطقه باز، کوهستان، تونل)
- فیلتر کالمن توسعه‌یافته (EKF)
- موتور شبیه‌سازی
- توابع تولید اشکال و جداول

### 2. demo_simulation.py
فایل نمایشی که ویژگی‌های کلیدی سیستم را نشان می‌دهد:
- مقایسه انواع تداخل سیگنال
- مقایسه سناریوهای محیطی
- تحلیل سه فاز تونل
- خصوصیات سیگنال GNSS
- عملکرد EKF

### 3. usage_examples.py
مثال‌های کاربردی برای استفاده از اجزای مختلف:
- شبیه‌سازی پایه
- تولید سیگنال GNSS
- اعمال تداخل
- استفاده از EKF
- سناریوهای محیطی
- شبیه‌سازی سفارشی
- تجسم‌سازی سفارشی

## اشکال تولید شده

### اشکال اصلی مقاله (Figures 1-10)
1. **figure_1_framework.png** - چارچوب مدل‌سازی سیستم
2. **figure_2_hierarchy.png** - معماری سلسله‌مراتبی CPN
3. **figure_3_top_level.png** - مدل CPN سطح بالا
4. **figure_4_gnss_receiver.png** - ماژول گیرنده GNSS
5. **figure_5_open_area.png** - زیرماژول منطقه باز
6. **figure_6_mountain.png** - زیرماژول کوهستان
7. **figure_7_tunnel.png** - زیرماژول تونل
8. **figure_8_position_solution.png** - ماژول حل موقعیت
9. **figure_9_evaluation.png** - ماژول ارزیابی
10. **figure_10_tunnel_errors.png** - خطاهای سناریو تونل

### اشکال اضافی
- **automaton_state_machine.png** - نمودار ماشین حالت اتوماتون
- **comparison_interference_errors.png** - مقایسه خطاهای تداخل
- **comparison_environment_errors.png** - مقایسه خطاهای محیطی
- **table_4_interference_performance.png** - جدول عملکرد تداخل
- **table_5_environment_performance.png** - جدول عملکرد محیطی

## جداول تولید شده

### جدول 1 - تعریف مجموعه‌های رنگی (Colsets)
شامل 18 نوع داده مختلف:
- BOOL, INT, REAL, SIGNAL, SCENARIO, و غیره

### جدول 2 - اعلان متغیرها (Variables)
شامل تمام متغیرهای استفاده شده در مدل CPN

### جدول 3 - گزارش فضای حالت (State Space Report)
- Nodes: 5365
- Arcs: 6410
- Dead markings: 648

### جدول 4 - عملکرد تحت تداخل‌های مختلف
مقایسه خطای موقعیت‌یابی در:
- حالت عادی (بدون تداخل)
- تداخل AM
- تداخل FM
- تداخل پالسی

### جدول 5 - عملکرد تحت سناریوهای مختلف محیطی
مقایسه خطای موقعیت‌یابی در:
- منطقه باز
- انسداد کوهستانی
- تونل

## مدل‌های ریاضی پیاده‌سازی شده

### 1. مدل سیگنال GNSS
```
ρ = √[(x_sat - x_recv)² + (y_sat - y_recv)² + (z_sat - z_recv)²] + c·δt + ε
```

### 2. تداخل AM
```
I_AM(t) = [1 + m·sin(2πf_e·t)] · sin(2πf_c·t)
```
- m = 0.5 (عمق مدولاسیون)
- f_e = 1 Hz (فرکانس پوشش)
- f_c = 1575.42 MHz (فرکانس حامل)

### 3. تداخل FM
```
I_FM(t) = [sin(2π(f_c + Δf)t) + sin(2π(f_c - Δf)t)] / 2
```
- Δf ~ N(0, 75 kHz) (انحراف فرکانس)

### 4. تداخل پالسی
```
I_pulse(t) = A · rect((t mod T_p) / T_p)
```
- A ∈ [1, 5] (دامنه تصادفی)

### 5. فیلتر کالمن توسعه‌یافته (EKF)

**بردار حالت:** x = [x, y, z, vx, vy, vz, δt]ᵀ

**مرحله پیش‌بینی:**
```
x̂_k|k-1 = F·x̂_k-1|k-1
P_k|k-1 = F·P_k-1|k-1·Fᵀ + Q
```

**مرحله به‌روزرسانی:**
```
K_k = P_k|k-1·Hᵀ·(H·P_k|k-1·Hᵀ + R)⁻¹
x̂_k|k = x̂_k|k-1 + K_k·(z_k - h(x̂_k|k-1))
P_k|k = (I - K_k·H)·P_k|k-1
```

## اجزای شبکه پتری رنگی

### مکان‌ها (Places)
- مکان‌های داده ورودی
- مکان‌های حالت پردازش
- مکان‌های نتیجه خروجی
- مکان‌های کنترل

### انتقال‌ها (Transitions)
- انتقال‌های پردازش سیگنال
- انتقال‌های انتخاب سناریو
- انتقال‌های محاسبه خطا
- انتقال‌های محاسبه EKF

### نشانه‌ها (Tokens)
- داده سیگنال GNSS
- تخمین‌های موقعیت
- اندازه‌گیری خطاها
- پرچم‌های کنترل

## حالت‌های اتوماتون

1. **Initial** - مقداردهی اولیه سیستم
2. **Receiving Signals** - دریافت سیگنال GNSS
3. **Processing** - پردازش سیگنال
4. **Open Area** - پردازش منطقه باز
5. **Mountain** - پردازش سناریو کوهستان
6. **Tunnel** - پردازش سناریو تونل
7. **Position Solution** - محاسبه موقعیت EKF

## نحوه اجرا

### اجرای شبیه‌سازی کامل
```bash
python3 gnss_train_positioning_simulation.py
```
این دستور تمام اشکال و جداول مقاله را تولید می‌کند.

### اجرای نمایش
```bash
python3 demo_simulation.py
```
این دستور ویژگی‌های کلیدی را نمایش می‌دهد.

### اجرای مثال‌های کاربردی
```bash
python3 usage_examples.py
```
این دستور نحوه استفاده از اجزای مختلف را نشان می‌دهد.

## نتایج کلیدی

### تأثیر تداخل سیگنال
- تداخل FM شدیدترین تأثیر را دارد
- تداخل AM و پالسی تأثیر متوسطی دارند
- خطای میانگین در حالت عادی: ~1.03 متر
- خطای میانگین با تداخل FM: ~6.22 متر

### تأثیر محیط
- منطقه باز: کمترین خطا (~1.03 متر)
- کوهستان: خطای متوسط (~1.30 متر)
- تونل: بیشترین خطا (~5.67 متر)

### سه فاز تونل
1. **داخل تونل**: از دست رفتن کامل سیگنال
2. **بلافاصله پس از خروج**: خطای بالا به دلیل چندمسیری
3. **پایدارسازی**: کاهش تدریجی خطا

## بسته‌های مورد نیاز

```
numpy>=1.20.0
matplotlib>=3.3.0
pandas>=1.2.0
PyPDF2>=3.0.0
```

## نتیجه‌گیری

این پیاده‌سازی یک شبیه‌سازی کامل و جامع از سیستم موقعیت‌یابی قطار مبتنی بر GNSS با شبکه‌های پتری رنگی و اتوماتون ارائه می‌دهد. تمام اشکال، جداول و مدل‌های ریاضی مقاله به طور دقیق پیاده‌سازی و بازتولید شده‌اند.

### ویژگی‌های برجسته
✅ پیاده‌سازی کامل مدل CPN  
✅ شبیه‌سازی دقیق سیگنال‌های GNSS  
✅ مدل‌سازی سه نوع تداخل (AM, FM, Pulse)  
✅ پیاده‌سازی سه سناریو محیطی  
✅ فیلتر کالمن توسعه‌یافته کامل  
✅ تولید تمام اشکال و جداول مقاله  
✅ ماشین حالت اتوماتون  
✅ مستندات جامع فارسی و انگلیسی  

## مراجع

Chen, S., Wu, D., Liu, J., & Wang, S. (2025). Modeling and performance analysis of GNSS-based train positioning system with colored petri nets. High-speed Railway, 3, 175-184.
